[build]
  command = "cd src/frontend && pnpm install && pnpm build"
  publish = "src/frontend/dist"

[build.environment]
  NODE_VERSION = "18"
  PNPM_VERSION = "10.33.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
